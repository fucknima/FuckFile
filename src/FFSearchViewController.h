#import <UIKit/UIKit.h>

// Global recursive search over the MHA virtual root, with search
// history on the idle screen and live batched results.
@interface FFSearchViewController : UITableViewController

@end
