#import <UIKit/UIKit.h>

NS_ASSUME_NONNULL_BEGIN

// 设置 → 文件查看 → 支持的文件查看器：所有注册 Viewer 的元数据与
// 能力说明（含不支持格式的诚实标注），并提供「恢复默认关联」。
@interface FFSupportedViewersViewController : UITableViewController

@end

NS_ASSUME_NONNULL_END
