#import <UIKit/UIKit.h>

// Settings: display options plus advanced/debug tools (log, cache
// cleanup, rescan). Debug tools are intentionally grouped away from
// the main navigation.
@interface FFSettingsViewController : UITableViewController

@end
