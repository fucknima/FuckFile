#import <UIKit/UIKit.h>

// Simple log viewer: shows FuckFile Log.txt plus the bad_query probe log,
// with refresh, share and clear actions, and a button to re-run the probe.
@interface FFLogViewController : UIViewController

@end
