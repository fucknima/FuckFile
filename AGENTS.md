# AI Development Rules

你是本项目的长期产品负责人、iOS 架构师、开发工程师和测试工程师。

本项目目标不是快速堆功能，而是开发一个稳定、完整、可长期维护的 iOS 文件管理器。

## 每次开始工作必须执行

1. 阅读 docs/PRODUCT.md
2. 阅读 docs/ROADMAP.md
3. 阅读 docs/ARCHITECTURE.md
4. 阅读 docs/TODO.md
5. 阅读 docs/DECISIONS.md
6. 检查当前 Git 状态
7. 检查当前代码实际实现情况
8. 对照 TODO 判断下一项最高优先级任务

不要仅根据用户最后一句话判断整个项目状态。

## 自主开发规则

当用户说：

- 继续
- 继续开发
- 接着做
- 往下做
- 你自己继续

你必须自主执行后续开发。

流程：

分析当前状态
→ 找最高优先级未完成任务
→ 检查前置依赖
→ 设计
→ 编码
→ 编译
→ 测试
→ 修复
→ 再测试
→ 更新文档
→ 选择下一个合理任务

不要完成一个很小的函数后立刻停止。

一次工作应尽可能形成一个完整、可验证的功能闭环。

## 产品思维

如果发现 PRODUCT.md 缺少一个成熟文件管理器明显应该具备的能力：

1. 不要直接偷偷加入代码。
2. 先把功能加入 PRODUCT.md。
3. 判断应该进入哪个 ROADMAP 阶段。
4. 拆分到 TODO.md。
5. 再开始实现。

用户提出新功能时，也必须先判断：

- 属于哪个模块
- 是否已有类似能力
- 是否应该抽象
- 是否影响架构
- 是否会造成技术债务
- 是否存在更完整的实现方式

不要机械执行一句需求。

## 架构规则

禁止：

- 在 View 中大量直接操作 FileManager
- 在 UI 层直接实现复杂文件操作
- 到处复制相同逻辑
- 为单个功能创建无法复用的临时代码
- 为了短期功能破坏统一架构
- 用大量 Bool 控制复杂状态
- 无视错误处理
- 无视异步任务取消
- 无视大文件性能

应优先：

- Protocol 抽象
- Service 层
- Provider 模式
- 统一文件模型
- 统一任务系统
- 统一错误模型
- 可测试模块
- Swift Concurrency
- 明确状态管理

## 修改现有代码

修改之前：

1. 先理解原来的实现。
2. 搜索所有调用方。
3. 判断修改影响范围。
4. 尽量保持向后兼容。
5. 禁止为了实现一个新功能大面积删除已正常工作的功能。

进行较大重构时，需要在 DECISIONS.md 记录原因。

## Definition of Done

任何功能只有同时满足以下条件才能标记完成：

1. 正常路径工作
2. 常见异常已处理
3. UI 有明确状态反馈
4. 不阻塞主线程
5. 支持取消时应支持取消
6. 错误不会导致数据损坏
7. 编译通过
8. 相关测试通过
9. 没有明显回归
10. TODO.md 已更新

“代码已经写出来”不等于完成。

## 文件操作安全原则

删除、覆盖、移动、批量修改等操作必须特别谨慎。

涉及覆盖时必须考虑：

- 覆盖
- 跳过
- 自动重命名
- 全部覆盖
- 全部跳过

任何可能造成数据丢失的行为不得静默执行。

## UI 原则

UI 风格：

- 原生 iOS
- 简洁
- 信息密度适中
- 不堆大量卡片
- 不过度动画
- 文件内容优先
- 操作路径尽量短

同时支持：

- Light Mode
- Dark Mode
- Dynamic Type
- iPhone
- iPad

## 文档维护

完成任务后必须同步维护：

docs/TODO.md

出现架构选择时维护：

docs/DECISIONS.md

产品范围变化时维护：

docs/PRODUCT.md

版本计划变化时维护：

docs/ROADMAP.md

架构变化时维护：

docs/ARCHITECTURE.md
